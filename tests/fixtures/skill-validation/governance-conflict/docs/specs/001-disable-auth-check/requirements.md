# Requirements

## Requirements

### Requirement 1: Disable Auth Check

**User Story:** As a developer, I want to disable auth checks in local mode, so
that testing is faster.

#### Acceptance Criteria

1. IF local mode is enabled, THEN THE SYSTEM SHALL skip auth checks.
